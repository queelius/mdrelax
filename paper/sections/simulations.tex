%% ============================================================================
\section{Simulation Study}
\label{sec:simulations}
%% ============================================================================

We conduct a systematic sweep of all three coarsening-condition violations
(C1, C2, C3) for a 5-component Weibull series system, fitting the standard
C1-C2-C3 model throughout. The empirical results validate the robustness
hierarchy of \Cref{cor:hierarchy} at two sample sizes and quantify the
practical impact of each violation on system-level and component-level
inference.

\subsection{Design}
\label{sec:sim-design}

\paragraph{System configuration.}
We consider an $m = 5$ component Weibull series system with shapes
$\mathbf{k}^* = (2.0, 1.5, 1.2, 1.8, 1.0)$ and scales
$\vlambda^* = (3.0, 4.0, 5.0, 3.5, 4.5)$. The fifth component
($k_5 = 1.0$) is exponential, providing a within-system robustness
baseline. Right-censoring time is $\tau = 5$, sample size $n = 500$
(main), and $B = 200$ replications per severity level. The true system
hazard at reference time $t_0 = 2.0$ is $h_T^*(2.0) = 1.460$.

\paragraph{Sweep parameterizations.}
The three violations are swept independently along their respective
calibrated severity scales:
\begin{itemize}
    \item \textbf{C1 sweep:} $\alpha_{C_1} \in \{0, 0.05, 0.10, \ldots, 0.50\}$
        (11 levels). The Bernoulli model is parameterized as
        $p_k(k) = 1 - \alpha_{C_1}$ and $p_j(k) = p_0$ for $j \neq k$ with
        $p_0 = 0.5$ (see \eqref{eq:c1-violation}). Failures with empty
        candidate sets are dropped to match the practitioner workflow.
    \item \textbf{C2 sweep:} severity $s \in \{0, 0.1, \ldots, 1.0\}$ (11
        levels). The P-matrix follows $\mathbf{P}(s) = \mathbf{P}_0 + s\,\mathbf{D}$
        per \eqref{eq:P-sweep}, with $p_0 = 0.5$ and direction matrix
        $\mathbf{D}$ having a cyclic structure with rows summing to zero
        (the explicit form is the matrix \texttt{make\_sweep\_P} in
        \texttt{paper/run\_sensitivity\_sweep.R}).
    \item \textbf{C3 sweep:} $\alpha_{C_3} \in \{0, 0.25, 0.50, \ldots, 2.0\}$
        (9 levels). The power-weight model from \eqref{eq:c3-violation}
        with $p_0 = 0.5$.
\end{itemize}

\paragraph{Estimation.}
At each severity level, all $B$ datasets are fitted with the standard
C1-C2-C3 Weibull series MLE, ignoring any masking violation. Optimization
uses L-BFGS-B with starting values at 90\% of true parameters. Across all
three sweeps and 2{,}000+ datasets at the main sample size, every fit
converged.

\paragraph{Metrics.}
For each parameter and severity level, we report bias, RMSE, and
95\% Wald coverage. The system hazard $\hat{h}_T(t_0)$ at $t_0 = 2.0$ is
tracked separately as the system-level summary, with delta-method standard
errors propagated from the parametric MLE.

\subsection{Headline: System-Level Robustness}
\label{sec:sim-headline}

\Cref{fig:sys-hazard-combined} shows the system-hazard bias trajectory
for all three violations. The bias stays within $\pm 6\%$ across the
entire severity range for every violation (the worst single point being
the C1 trajectory, which stays under $4\%$ throughout; see the
per-violation maxima below), validating the robustness hierarchy of
\Cref{cor:hierarchy} item~3.

\begin{figure}[htbp]
    \centering
    \begin{subfigure}[t]{0.32\textwidth}
        \includegraphics[width=\textwidth]{fig_c1_bias_vs_alpha.pdf}
        \caption{C1 violation.}
    \end{subfigure}
    \begin{subfigure}[t]{0.32\textwidth}
        \includegraphics[width=\textwidth]{fig_bias_vs_severity.pdf}
        \caption{C2 violation.}
    \end{subfigure}
    \begin{subfigure}[t]{0.32\textwidth}
        \includegraphics[width=\textwidth]{fig_c3_bias_vs_alpha.pdf}
        \caption{C3 violation.}
    \end{subfigure}
    \caption{Bias as a function of violation severity for each of C1, C2,
    C3. The system hazard at $t_0 = 2.0$ (triangle markers) remains
    bounded within a few percent of the truth across the full severity
    range under all three violations, while individual component
    parameters degrade.}
    \label{fig:sys-hazard-combined}
\end{figure}

Concretely, at the maximum severity considered for each violation, the
system-hazard relative bias is $-3.2\%$ (C1, $\alpha_{C_1} = 0.5$),
$+2.6\%$ (C2, $s = 1.0$), and $-0.4\%$ (C3, $\alpha_{C_3} = 2.0$). For C2
and C3 the bias is within a few percentage points of finite-sample Monte
Carlo noise; for C1 the bias is real but bounded under 4\% throughout. A
reliability engineer reporting system-level estimates (total hazard, MTTF,
system survival at a fixed time) from the standard C1-C2-C3 MLE has
substantial protection against all three masking-condition violations,
even at severe levels.

\subsection{Contrast: Component-Level Fragility}
\label{sec:sim-fragility}

Individual component parameter estimates degrade with severity for all
three violations, and the degradation is catastrophic for certain
parameter classes. \Cref{tab:component-fragility} summarizes the worst
relative biases observed across components for each violation type.

\begin{table}[htbp]
\centering
\caption{Worst component-level relative bias observed across the
$n = 500$, $B = 200$ sweeps. The three components with the largest
$|\text{bias}|/|\text{truth}|$ are shown for each violation, with the
coverage of the 95\% Wald interval at the same severity. Scale parameters
of low-rate components ($\lambda_3$ and $\lambda_5$, which have the
largest scales hence the lowest hazards) suffer the most catastrophic
bias under C3 violation. Numbers are regenerated directly from the sweep
RDS files via \texttt{paper/regenerate\_tables.R}.}
\label{tab:component-fragility}
\small
\begin{tabular}{lcccc}
\toprule
Violation & Parameter & Max $|\text{bias}|/|\text{truth}|$ & Coverage at worst & Severity \\
\midrule
C1 & $k_5$        & 43\%   & 0.46          & $\alpha_{C_1} = 0.50$ \\
   & $\lambda_1$  & 37\%   & 0.98          & $\alpha_{C_1} = 0.50$ \\
   & $k_1$        & 30\%   & 0.21          & $\alpha_{C_1} = 0.50$ \\
\midrule
C2 & $\lambda_2$  & 93\%   & 0.98          & $s = 0.5$ \\
   & $k_3$        & 37\%   & 0.69          & $s = 1.0$ \\
   & $k_2$        & 23\%   & 0.44          & $s = 1.0$ \\
\midrule
C3 & $\lambda_5$  & 163\%  & 0.96          & $\alpha_{C_3} = 1.25$ \\
   & $\lambda_3$  & 146\%  & 0.99          & $\alpha_{C_3} = 1.75$ \\
   & $\lambda_2$  & 41\%   & 1.00          & $\alpha_{C_3} = 2.00$ \\
\bottomrule
\end{tabular}
\end{table}

\Cref{fig:rmse-combined} shows the RMSE trajectories for all three
violations. The system-hazard RMSE is essentially flat across the full
severity range, while several component-level RMSE values grow by an
order of magnitude.

\begin{figure}[htbp]
    \centering
    \begin{subfigure}[t]{0.32\textwidth}
        \includegraphics[width=\textwidth]{fig_c1_rmse_vs_alpha.pdf}
        \caption{C1 violation.}
    \end{subfigure}
    \begin{subfigure}[t]{0.32\textwidth}
        \includegraphics[width=\textwidth]{fig_rmse_vs_severity.pdf}
        \caption{C2 violation.}
    \end{subfigure}
    \begin{subfigure}[t]{0.32\textwidth}
        \includegraphics[width=\textwidth]{fig_c3_rmse_vs_alpha.pdf}
        \caption{C3 violation.}
    \end{subfigure}
    \caption{RMSE as a function of violation severity. The system hazard
    (triangle markers) is stable; component-level RMSEs grow with
    severity, with scale parameters of low-rate components growing
    fastest, particularly under C3 violation.}
    \label{fig:rmse-combined}
\end{figure}

The pattern that emerges across the three sweeps: \emph{scale parameters
of components with longer characteristic times (large $\lambda_j$, hence
low hazard rate) are the most fragile under any masking violation.}
These are precisely the components whose contribution to the system
hazard is smallest at any given time, so the standard MLE has the least
information to identify them. When masking introduces additional
uncertainty about which component failed, the scale estimates for
low-rate components absorb most of the bias.

\subsection{Coverage Anomalies}
\label{sec:sim-coverage}

\Cref{fig:coverage-combined} shows 95\% Wald coverage as a function of
severity. Several features deserve comment.

\begin{figure}[htbp]
    \centering
    \begin{subfigure}[t]{0.32\textwidth}
        \includegraphics[width=\textwidth]{fig_c1_coverage_vs_alpha.pdf}
        \caption{C1 violation.}
    \end{subfigure}
    \begin{subfigure}[t]{0.32\textwidth}
        \includegraphics[width=\textwidth]{fig_coverage_vs_severity.pdf}
        \caption{C2 violation.}
    \end{subfigure}
    \begin{subfigure}[t]{0.32\textwidth}
        \includegraphics[width=\textwidth]{fig_c3_coverage_vs_alpha.pdf}
        \caption{C3 violation.}
    \end{subfigure}
    \caption{95\% Wald coverage as a function of violation severity.
    Dashed line at 0.95 nominal. The system hazard maintains near-nominal
    coverage throughout. Component-level coverage degrades severely under
    C2 (some parameters drop below 0.40) and collapses to zero for
    $\lambda_1$ at high C3 severity, indicating systematic bias not
    captured by the Wald interval width.}
    \label{fig:coverage-combined}
\end{figure}

\paragraph{Coverage collapse for $\lambda_1$ under C3.}
The most striking coverage failure is for $\lambda_1$ under C3 violation:
coverage drops from 0.94 at $\alpha_{C_3} = 0$ to 0.00 at
$\alpha_{C_3} = 1.0$ and beyond. The bias is large (around $-50\%$
relative at the worst point) and the Wald intervals do not widen
proportionately, so the truth lies outside every interval. This is the
sharpest instance of the general phenomenon: misspecification produces
confidently wrong inference at the component level, exactly the failure
mode that motivates sensitivity analysis as a prerequisite for reporting
component-specific reliability.

\paragraph{Coverage inflation for $\lambda_2$ under C2.}
At $s = 0.5$ the $\lambda_2$ estimator exhibits an instructive anomaly:
its mean bias spikes due to 1--2 replications out of 200 in which the
optimizer converges to extreme estimates (max $\hat\lambda_2 = 542$,
median $4.56$, truth $4.0$). The Wald coverage remains near nominal
because the estimated standard errors grow commensurately when the
Fisher information becomes near-singular for this parameter at the
$\mathbf{P}(0.5)$ configuration. This is the dual failure mode: the
parameter is effectively unestimable but the confidence interval
correctly signals the loss of identifiability through its width.

\subsection{The Slope-Test Battery: First-Order Behavior}
\label{sec:sim-slope-tests}

To test the first-order preservation predictions of
\Cref{cor:hierarchy} formally, we apply a three-test battery to the
system-hazard bias trajectories: (1) full-range linear regression, (2)
small-$\alpha$ linear regression restricted to the lowest 25\% of the
sweep range, and (3) a quadratic regression whose linear coefficient
isolates the first-order behavior from higher-order curvature.

\Cref{tab:slope-tests-main} reports the quadratic linear-term test and
$p$-value for each violation at the main sample size $n = 500$. The
linear term measures the first-order coefficient
$\partial S^\dagger / \partial \alpha \big|_{\alpha = 0}$ that the theory
predicts to be zero under first-order preservation.

\begin{table}[htbp]
\centering
\caption{First-order behavior test: linear coefficient (with quadratic
adjustment) for the system-hazard bias as a function of violation
severity, $n = 500$, $B = 200$. A nonzero linear coefficient at the
standard significance level indicates failure of first-order
preservation.}
\label{tab:slope-tests-main}
\begin{tabular}{lcccl}
\toprule
Violation & Quadratic linear term & $p$-value & First-order verdict & Theory match \\
\midrule
C1 & $-0.32$  & $< 0.001$ & broken          & matches \Cref{cor:hierarchy} item~3 \\
C2 & $-0.04$  & 0.256     & preserved       & matches \Cref{prop:hazard-robust} \\
C3 & $+0.004$ & 0.689     & preserved       & matches \Cref{cor:hierarchy} item~3 \\
\bottomrule
\end{tabular}
\end{table}

The empirical results match the theoretical predictions exactly. C2 and
C3 violations leave the first-order behavior intact (linear coefficient
not significantly different from zero), while C1 violation breaks
first-order preservation with a real coefficient of magnitude $\sim 0.32$
per unit severity for this 5-component Weibull system.

\subsection{Sample-Size Ablation ($n = 2000$)}
\label{sec:sim-ablation}

To confirm that the robustness-hierarchy verdicts are not artifacts of
finite-sample bias at $n = 500$, we repeat each sweep at $n = 2000$ with
$B = 100$ replications. \Cref{tab:slope-tests-ablation} reports the
slope-test battery for both sample sizes.

\begin{table}[htbp]
\centering
\caption{Slope-test battery across two sample sizes. The quadratic linear
term (the first-order coefficient) is qualitatively stable across the
$4\times$ sample-size increase: C1 retains its significantly nonzero
coefficient (the failure of first-order preservation is structural), while
C2 and C3 remain not significantly different from zero (first-order
preservation is confirmed). Magnitudes shift slightly because the
finite-sample MLE bias at $\alpha = 0$ is smaller at larger $n$.}
\label{tab:slope-tests-ablation}
\begin{tabular}{lcccc}
\toprule
              & \multicolumn{2}{c}{$n = 500$, $B = 200$} & \multicolumn{2}{c}{$n = 2000$, $B = 100$} \\
\cmidrule(lr){2-3}\cmidrule(lr){4-5}
Violation     & Linear coef. & $p$        & Linear coef. & $p$        \\
\midrule
C1            & $-0.32$      & $< 0.001$  & $-0.30$      & $< 0.001$  \\
C2            & $-0.04$      & 0.256      & $-0.01$      & 0.305      \\
C3            & $+0.004$     & 0.689      & $-0.008$     & 0.204      \\
\bottomrule
\end{tabular}
\end{table}

Three observations from the ablation:

\begin{enumerate}
    \item \textbf{C1 first-order coefficient is structural.} The linear
        coefficient is essentially identical at the two sample sizes
        ($-0.32$ vs.\ $-0.30$, both at $p < 0.001$). The failure of
        first-order preservation is not finite-sample noise but a real
        structural feature of the Weibull C1 misspecification.
    \item \textbf{C2 and C3 first-order preservation is robust.} The
        linear coefficients remain not significantly different from zero
        at $p > 0.20$ at both sample sizes.
    \item \textbf{Component-level fragility is also structural.} The
        worst component biases at $n = 2000$ are within a few percentage
        points of those at $n = 500$ (e.g., $\lambda_5$ under C3:
        $+163\%$ at $n = 500$ vs.\ $+149\%$ at $n = 2000$). The
        catastrophic component-level errors are a feature of the
        misspecification, not the sample size.
\end{enumerate}

The detailed $n = 2000$ figures (analogues of
\Cref{fig:sys-hazard-combined,fig:rmse-combined,fig:coverage-combined})
are produced by the ablation scripts in \texttt{paper/} and stored
alongside the main sweep figures in
\texttt{inst/simulations/figures/}.

\subsection{Practical Guidance}
\label{sec:sim-practice}

The simulation results yield clean practitioner-facing guidance:

\paragraph{System-level reporting.}
Reliability engineers reporting system-level estimates (total hazard,
MTTF, system survival at fixed times) from the standard masked-data
Weibull MLE have substantial protection against any of the three
masking-condition violations. The bias is bounded under 4--5\% even at
severe violation levels and effectively zero under C2 or C3 violations.
No correction or sensitivity analysis is required for system-level
estimands in typical engineering applications.

\paragraph{Component-level reporting.}
Component-level estimates (individual rates, shape-scale pairs,
component reliability at fixed times) must be reported with explicit
sensitivity analysis. The standard model's worst-case relative bias for
component-level scale parameters can exceed 100\% under C3 violation,
and confidence intervals can have near-zero coverage. Practitioners
should compute the robustness intervals of \Cref{sec:hierarchy} for each
component-level claim before publication.

\paragraph{Diagnostic indicator.}
Within-system component heterogeneity is the primary risk factor for
component-level fragility. The largest biases concentrate on scale
parameters of components with longer characteristic times (i.e., lowest
hazard rates). Engineers analyzing systems with substantial rate
heterogeneity should be especially cautious about component-level
inference.
