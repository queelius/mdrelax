% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/md_candidate_set_models.R
\name{md_loglike_exp_series_C1_C2_C3}
\alias{md_loglike_exp_series_C1_C2_C3}
\title{Log-likelihood function for exponential series system with masked data (C1,C2,C3)}
\usage{
md_loglike_exp_series_C1_C2_C3(
  md,
  sysvar = "t",
  setvar = "x",
  deltavar = "delta"
)
}
\arguments{
\item{md}{Masked data frame containing:
\itemize{
\item System lifetime column (default: "t")
\item Candidate set columns (default: "x1", "x2", ..., "xm")
\item Optional right-censoring indicator (default: "delta")
}}

\item{sysvar}{Column name for system lifetime. Default is "t".}

\item{setvar}{Column prefix for candidate set (Boolean matrix). Default is "x".}

\item{deltavar}{Column name for right-censoring indicator. Default is "delta".
If NULL or column doesn't exist, assumes no censoring.}
}
\value{
A function \code{f(theta)} that computes the log-likelihood given
rate parameters \verb{theta = (lambda_1, ..., lambda_m)}.
}
\description{
Returns a log-likelihood function for an exponential series system with masked
component cause of failure under candidate sets satisfying conditions C1, C2,
and C3.
}
\details{
For a series system with m components having exponential lifetimes with rate
parameters \verb{theta = (lambda_1, ..., lambda_m)}:
\itemize{
\item System reliability: \code{R(t) = exp(-sum(theta) * t)}
\item System hazard: \code{h(t) = sum(theta)}
}

For observation i with system lifetime \code{s_i}, right-censoring indicator \code{delta_i},
and candidate set \code{C_i}:
\itemize{
\item If uncensored (delta_i = FALSE): \verb{L_i(theta) = R(s_i) * sum_\{j in C_i\} h_j(s_i)}
\item If censored (delta_i = TRUE): \code{L_i(theta) = R(s_i)}
}

The log-likelihood is:
\deqn{\ell(\theta) = \sum_i \log L_i(\theta)}

For uncensored observations:
\deqn{\log L_i(\theta) = -s_i \sum_{j=1}^m \lambda_j + \log\left(\sum_{j \in C_i} \lambda_j\right)}

For censored observations:
\deqn{\log L_i(\theta) = -s_i \sum_{j=1}^m \lambda_j}
}
\examples{
\dontrun{
# Generate some masked data
md <- tibble::tibble(
  t = c(0.5, 1.2, 0.8),
  x1 = c(TRUE, TRUE, FALSE),
  x2 = c(TRUE, FALSE, TRUE),
  x3 = c(FALSE, TRUE, TRUE),
  delta = c(FALSE, FALSE, TRUE)
)
ll <- md_loglike_exp_series_C1_C2_C3(md)
ll(c(1, 1.5, 2))  # Evaluate at theta = (1, 1.5, 2)
}

}
