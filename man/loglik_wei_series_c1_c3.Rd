% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wei_series_c1_c3.R
\name{loglik_wei_series_c1_c3}
\alias{loglik_wei_series_c1_c3}
\title{Log-likelihood for Weibull series system with masked data (C1, C3) - Relaxed C2}
\usage{
loglik_wei_series_c1_c3(t, C, delta = NULL, P)
}
\arguments{
\item{t}{Numeric vector of system lifetimes}

\item{C}{Logical matrix of candidate sets (n x m)}

\item{delta}{Censoring indicators (1=observed, 0=censored)}

\item{P}{Inclusion probability matrix (m x m): \verb{P[j,k] = P(j in C | K = k)}.
Diagonal must be 1 (C1 condition).}
}
\value{
Function that takes theta = c(k1,lambda1,k2,lambda2,...) and returns log-likelihood
}
\description{
Returns a log-likelihood function for masked data from a Weibull series
system under conditions C1 and C3, with relaxed C2 (informative masking).
}
\details{
The log-likelihood is:
\verb{l(theta) = sum_i [-sum_j (t_i/lambda_j)^k_j] + sum_\{i:delta_i=1\} log(sum_\{j in C_i\} h_j(t_i) * pi_j(c_i))}

where h_j(t) = (k_j/lambda_j)(t/lambda_j)^(k_j-1) is the Weibull hazard, and
pi_j(c_i) = P(C = c_i | K = j) computed from the general Bernoulli model.
}
