% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/md_candidate_set_models.R
\name{md_score_exp_series_C1_C2_C3}
\alias{md_score_exp_series_C1_C2_C3}
\title{Score function (gradient of log-likelihood) for exponential series system (C1,C2,C3)}
\usage{
md_score_exp_series_C1_C2_C3(
  md,
  sysvar = "t",
  setvar = "x",
  deltavar = "delta"
)
}
\arguments{
\item{md}{Masked data frame containing:
\itemize{
\item System lifetime column (default: "t")
\item Candidate set columns (default: "x1", "x2", ..., "xm")
\item Optional right-censoring indicator (default: "delta")
}}

\item{sysvar}{Column name for system lifetime. Default is "t".}

\item{setvar}{Column prefix for candidate set (Boolean matrix). Default is "x".}

\item{deltavar}{Column name for right-censoring indicator. Default is "delta".
If NULL or column doesn't exist, assumes no censoring.}
}
\value{
A function \code{g(theta)} that computes the score (gradient) vector
at parameter values \verb{theta = (lambda_1, ..., lambda_m)}.
}
\description{
Returns the score function (gradient of log-likelihood with respect to theta)
for an exponential series system with masked data under C1, C2, C3 conditions.
}
\details{
The score for component j is:
\deqn{\frac{\partial \ell}{\partial \lambda_j} = -\sum_i s_i + \sum_{i: \delta_i=0} \frac{I(j \in C_i)}{\sum_{k \in C_i} \lambda_k}}

where \verb{I(j in C_i)} is 1 if component j is in candidate set C_i, 0 otherwise.
}
\examples{
\dontrun{
md <- tibble::tibble(
  t = c(0.5, 1.2, 0.8),
  x1 = c(TRUE, TRUE, FALSE),
  x2 = c(TRUE, FALSE, TRUE),
  x3 = c(FALSE, TRUE, TRUE)
)
score <- md_score_exp_series_C1_C2_C3(md)
score(c(1, 1.5, 2))
}

}
