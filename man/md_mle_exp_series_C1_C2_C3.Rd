% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/md_candidate_set_models.R
\name{md_mle_exp_series_C1_C2_C3}
\alias{md_mle_exp_series_C1_C2_C3}
\title{Maximum likelihood estimator for exponential series system (C1,C2,C3)}
\usage{
md_mle_exp_series_C1_C2_C3(
  md,
  theta0 = NULL,
  sysvar = "t",
  setvar = "x",
  deltavar = "delta",
  use_annealing = FALSE,
  control = list(),
  lower = 1e-10,
  upper = Inf,
  hessian = TRUE
)
}
\arguments{
\item{md}{Masked data frame containing:
\itemize{
\item System lifetime column (default: "t")
\item Candidate set columns (default: "x1", "x2", ..., "xm")
\item Optional right-censoring indicator (default: "delta")
}}

\item{theta0}{Initial parameter values for optimization. If NULL, uses
the method of moments estimator based on total system hazard.}

\item{sysvar}{Column name for system lifetime. Default is "t".}

\item{setvar}{Column prefix for candidate set (Boolean matrix). Default is "x".}

\item{deltavar}{Column name for right-censoring indicator. Default is "delta".
If NULL or column doesn't exist, assumes no censoring.}

\item{use_annealing}{Logical. If TRUE, uses simulated annealing to find
a good starting point before local optimization. Default is FALSE.}

\item{control}{List of control parameters passed to optim(). Default uses
L-BFGS-B with reasonable settings.}

\item{lower}{Lower bounds for parameters. Default is 1e-10.}

\item{upper}{Upper bounds for parameters. Default is Inf.}

\item{hessian}{Logical. If TRUE, compute Hessian at the MLE. Default is TRUE.}
}
\value{
An object of class \code{mle} (from \code{algebraic.mle} package) containing:
\itemize{
\item \code{theta.hat}: The MLE of rate parameters
\item \code{sigma}: Estimated variance-covariance matrix
\item \code{loglike}: Log-likelihood at the MLE
\item \code{info}: Observed Fisher information matrix
\item \code{nobs}: Number of observations
\item \code{converged}: Whether optimization converged
}
}
\description{
Computes the MLE of component failure rates for an exponential series
system with masked data under C1, C2, C3 candidate set conditions.
}
\details{
Uses L-BFGS-B optimization with optional simulated annealing for
finding a good starting point.

The MLE for exponential series systems with masked data has a closed-form
solution only in special cases. In general, numerical optimization is
required. This function uses the L-BFGS-B algorithm with analytically
computed gradients for efficiency.
}
\examples{
\dontrun{
# Generate masked data
library(tibble)
set.seed(42)
n <- 100
theta_true <- c(1, 1.5, 2)
# ... (data generation code)

mle <- md_mle_exp_series_C1_C2_C3(md, theta0 = theta_true)
print(mle)
confint(mle)
}

}
