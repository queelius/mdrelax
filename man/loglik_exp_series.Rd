% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exp_series_c1_c2_c3.R
\name{loglik_exp_series}
\alias{loglik_exp_series}
\title{Log-likelihood for exponential series system (C1, C2, C3)}
\usage{
loglik_exp_series(t, C, delta = NULL)
}
\arguments{
\item{t}{Numeric vector of system lifetimes}

\item{C}{Logical matrix of candidate sets (n x m), TRUE if component in set}

\item{delta}{Numeric/logical vector of censoring indicators (1=observed, 0=censored).
If NULL, assumes all observations are uncensored.}
}
\value{
Function that takes theta (rate parameters) and returns log-likelihood
}
\description{
Returns a log-likelihood function for masked data from an exponential series
system under conditions C1, C2, C3.
}
\examples{
t <- c(0.5, 1.0, 0.8)
C <- matrix(c(TRUE, TRUE, FALSE,
              TRUE, FALSE, TRUE,
              FALSE, TRUE, TRUE), nrow = 3, byrow = TRUE)
ll <- loglik_exp_series(t, C)
ll(c(1, 1.5, 2))
}
