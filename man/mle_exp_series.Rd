% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exp_series_c1_c2_c3.R
\name{mle_exp_series}
\alias{mle_exp_series}
\title{MLE for exponential series system (C1, C2, C3)}
\usage{
mle_exp_series(t, C, delta = NULL, theta0 = NULL, lower = 1e-10, upper = Inf)
}
\arguments{
\item{t}{Numeric vector of system lifetimes}

\item{C}{Logical matrix of candidate sets (n x m), TRUE if component in set}

\item{delta}{Numeric/logical vector of censoring indicators (1=observed, 0=censored).
If NULL, assumes all observations are uncensored.}

\item{theta0}{Initial parameter values. If NULL, uses method of moments.}

\item{lower}{Lower bound for parameters (default 1e-10)}

\item{upper}{Upper bound for parameters (default Inf)}
}
\value{
List with components:
\itemize{
\item \code{theta}: MLE of rate parameters
\item \code{se}: Standard errors (from FIM)
\item \code{vcov}: Variance-covariance matrix
\item \code{loglik}: Log-likelihood at MLE
\item \code{converged}: Logical, did optimization converge?
\item \code{fim}: Fisher information matrix at MLE
}
}
\description{
Computes maximum likelihood estimates of component failure rates.
}
\examples{
\dontrun{
# Generate data
set.seed(42)
sim <- rexp_series_md(n = 200, theta = c(1, 1.5, 2), p = 0.3, tau = 2)
fit <- mle_exp_series(sim$t, sim$C, sim$delta)
print(fit$theta)
}
}
