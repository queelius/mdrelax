% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exp_series_c1_c2_c3.R
\name{rexp_series_md}
\alias{rexp_series_md}
\title{Generate masked data from exponential series system}
\usage{
rexp_series_md(n, theta, p, tau, keep_latent = FALSE)
}
\arguments{
\item{n}{Sample size}

\item{theta}{Rate parameters for exponential component lifetimes}

\item{p}{Probability each non-failed component is in candidate set}

\item{tau}{Right-censoring time (scalar or vector of length n)}

\item{keep_latent}{If TRUE, include latent component times in output}
}
\value{
List with components:
\itemize{
\item \code{t}: System lifetimes (possibly censored)
\item \code{delta}: Censoring indicator (1 = observed failure, 0 = censored)
\item \code{C}: Candidate set matrix (n x m logical)
\item \code{k}: True failed component (latent)
\item \code{Tm}: Component lifetimes matrix (if keep_latent = TRUE)
}
}
\description{
Simulates component lifetimes, applies series system structure, right-censoring,
and generates candidate sets using Bernoulli model (C1, C2, C3).
}
\examples{
set.seed(42)
sim <- rexp_series_md(n = 100, theta = c(1, 1.5, 2), p = 0.3, tau = 2)
str(sim)
}
